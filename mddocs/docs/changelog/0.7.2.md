# 0.7.2 (2026-09-11) { #0.7.2 }

## Improvements

Adjust `fillfactor` for tables with large number of `UPDATE`s.

## Bug Fixes

Stop skipping tables with names starting with `all_` and `user_` - they look like Oracle system views, but may be user-defined in other DBMS.
